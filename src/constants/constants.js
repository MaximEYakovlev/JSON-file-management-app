// events / toast / messages
export const FILE_SAVED =
    "The file has been successfully saved to the browser's local storage.";

export const EXTENSION_JSON =
    'Please select the file with the extension .json.';

// events / toast / icons
export const THEME = 'colored';

// button
export const REACT_TABLE = 'react table';
export const SPREAD_SHEET = 'spread sheet';

// input
export const TYPE_FILE = 'file';
export const TYPE_TEXT = 'text';

// placeholder
export const PLACEHOLDER_FILTER = 'filter';

// sort order
export const ASC = 'asc';
export const DESC = 'desc';

// localStorage
export const LOCALSTORAGE_KEY = 'uploaded JSON';

// file type
export const APPLICATION_JSON = 'application/json';

// data types
export const STRING = 'string';
